"Day 1 ive learned about git commands such as clone-clones a repo to your local system, add - which stages your repo depending on the folder :use '.' for everything specified, commit - which takes your staged changes (the snapshot of them) with -m meaning message quotes double for the message, push - syncs those changes."

